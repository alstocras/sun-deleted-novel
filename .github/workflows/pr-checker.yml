name: PR Title Check

on:
  pull_request:
    branches: ["main"]

jobs:
  check-title:
    runs-on: ubuntu-latest

    steps:
      - name: Check PR title format
        run: |
          TITLE="${{ github.event.pull_request.title }}"
          echo "PR Title: $TITLE"

          if [[ "$TITLE" == chapter:* || "$TITLE" == scene:* || "$TITLE" == patch:* ]]; then
            echo "Valid title. proceed"
          else
            echo "bro READ THE README"
            echo "Title must start with: major:, scene:, or patch:"
            exit 1
          fi
